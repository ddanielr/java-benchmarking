# java-benchmarking
General holding repo for benchmarking code


## Building the code 
mvn clean package

## Running the code 
java -jar target/benchmarks.jar  Multiplexer* -gc true -prof gc -rf text -rff benchmark-results.txt

